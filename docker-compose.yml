version: '3.8'

services:
  pagekit:
    image: pagekit:latest
    restart: unless-stopped
    ports:
      - "13080:80"       # Nginx：已发布静态站点
      - "13000:3000"   # 管理后台
    volumes:
      - ./config.docker.json:/app/config.json:ro
      - ./data:/data

